node_modules/
dist/
tmp-screenshots/
.playwright-cli/
.specflow/

# L4 compose path (cortex#2095): the filled .env carries the Discord bot token
# and Claude OAuth token — never commit it. The .env.example placeholder file IS
# tracked.
deploy/compose/.env

# Container bench staging dir (cortex#2284): byte-copies of the compose inputs
# plus the placeholder fixture staged as `.env` — regenerated every run.
deploy/test/.work/
